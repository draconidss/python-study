import unittest


class List(unittest.TestCase):
    def test_list(self):
        l = [1, 'string', 3, 'string', 5]
        print(l)
        # 添加元素
        l.append(7)
        print(l)
        self.assertEqual(True, True)
        # 列表是有序集合，可以随时添加和删除其中的元素
        # 列表用方括号 [] 表示，元素之间用逗号分隔
        # 列表中的元素可以是不同类型的，也可以是列表
        # 列表中的元素可以是不同类型的，也可以是列表
        # 列表中的元素可以是不同类型的，也可以是列表
        # 列表中的元素可以是不同类型的，也可以是列表
        # 列表中的元素可以是不同类型的，也可以是列表
        # 列表中的元素可以是不同类型的，也可以是列表
        # 列表中的元素可以是不同类型的，��
